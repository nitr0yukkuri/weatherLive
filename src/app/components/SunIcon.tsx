'use client';

import { FiSun } from 'react-icons/fi';

export default function SunIcon() {
    return (
        <FiSun
            size={48}
            className="text-orange-400"
            strokeWidth={1}
        />
    );
}